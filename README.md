Sudoku solver I wrote in C++ using ncurses. It uses backtracking to solve the puzzle and allows the user to input the puzzle through the terminal. The user can also choose to solve a predefined puzzle or input their own. The program will display the solved puzzle in the terminal once it is complete.


